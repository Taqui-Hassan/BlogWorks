Blog Platform with Custom Authentication (MERN Stack)

A full-stack blogging application built using React, Redux, Node.js, Express, and MongoDB.
The project implements a complete authentication system using JWT access and refresh tokens, secure password hashing, protected routes, and user state management. Blog CRUD functionality and media uploads are in active development.

